--> sudo ssh-agent bash -c 'ssh-add /home/ubuntu/keys/frontend-app; git clone git@github.com:rajcryptonaut/cognofi.app.git'
--> netstat -plant | grep 80
--> readlink -f <filename> 
--> curl -ISL https://dev.jenkins.cognofi.app
--> sudo netstat -tunlp

#these command use to check the port open in linux 
sudo netstat -tuln			
sudo lsof -i -P -n | grep LISTEN

#this comand use to check the size of the folder 
sudo du -sh <filename>

#this command use to list the latest files on the end 
ls -latrh

# use A if terminal prompt you for confirmation
unzip ju87934.zip
(if) it require confirmation in yes and no then type A
and enter